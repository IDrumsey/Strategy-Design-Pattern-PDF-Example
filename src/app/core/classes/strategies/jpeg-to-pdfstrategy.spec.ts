import { JpegToPDFStrategy } from './jpeg-to-pdfstrategy';

describe('JpegToPDFStrategy', () => {
  it('should create an instance', () => {
    expect(new JpegToPDFStrategy()).toBeTruthy();
  });
});
